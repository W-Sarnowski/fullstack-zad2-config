apiVersion: apps/v1
kind: Deployment
metadata:
  name: zad2-deployment
  namespace: zad2-ns
  labels:
    app: zad2-server
spec:
  replicas: 4
  selector:
    matchLabels:
      app: zad2-server
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavaialable: 2
      maxSurge: 5
  template:
    metadata:
      labels:
        app: zad2-server
    spec:
      containers:
      - name: zad2-server
        image: scoutertf2/zad2:8b6f71b416aa51ef15ba2b89f37dbcafc1431f60
      dnsPolicy: ClusterFirst
      restartPolicy: Always
